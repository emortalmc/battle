apiVersion: autoscaling.agones.dev/v1
kind: FleetAutoscaler
metadata:
  name: battle
  namespace: emortalmc

spec:
  fleetName: battle
  policy:
    type: Buffer
    buffer:
      bufferSize: 1
      minReplicas: 3
      maxReplicas: 5
